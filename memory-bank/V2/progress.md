## 2026-05-11

### Step 1 Completed

- 已重做首页的手机端布局，让 393 × 740 视口下的统计区、`+ 记一笔` 和最近记录形成稳定的单屏节奏
- 已将首页统计区压缩为更紧凑的主视觉块，预算环改为卡片内占位，避免在手机尺寸下溢出统计框
- 已将首页中间动作区改为页面正中间的过渡带布局，让 `+ 记一笔` 成为首页视觉锚点，并尽量消化页面空白
- 已把首页最近记录改为紧凑密度，只保留最近 3 条的可见空间，避免记录区被拉成大块留白
- 已继续收紧全局壳层、首页视图和记录列表的手机端间距，保证页面在窄屏下仍能完整显示
- 已同步收紧记账抽屉在手机端的表单密度，确保保存后自动收起，便于连续记账

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- 已在 in-app browser 中复核 393 × 740 的首页效果，确认预算环回到统计区内，最近 3 条可完整展示，`+ 记一笔` 位于页面中部

### Notes

- 首页当前的核心目标是“单屏展示 + 中间动作锚点 + 最近三条直出”
- `LedgerHomeView` 现在通过紧凑密度的 `LedgerRecentRecordsList` 专门服务首页，而不是复用全部记录页的同一高度策略
- 后续若要继续扩展统计环或更多首页模块，需要优先维持 393 × 740 的可视完整性，避免再次把最近记录区挤出屏幕

### Step 2 Completed

- 已定义 V2 新增的核心领域词汇与数据模型，补齐了月度预算、分类预算、预算进度摘要、截图导入草稿、快捷指令导入来源和更多菜单动作
- 已将预算相关的固定词汇集中到 `ledger.constants.ts`，避免后续在页面里临时拼装预算状态、草稿来源和更多菜单动作字符串
- 已将预算设置、预算进度摘要、分类预算映射、草稿结构和更多菜单动作类型集中到 `src/types/ledger.ts`
- 已保留现有记账记录模型不变，确保本步只是在既有领域上新增 V2 模型层，不提前进入校验、存储或计算实现

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- 已检查新增类型和常量是否覆盖设计文档中的预算与草稿字段，并确认没有重复散落的关键业务字符串

### Notes

- 这一步只完成“模型与词汇定义”，还没有开始预算校验、存储或统计计算
- 后续第 3 步可以直接依赖本步定义的类型和常量来做预算与草稿校验
- `LedgerBudgetSettings` 和 `LedgerImportDraft` 已经成为后续预算页、截图导入和草稿确认流程的统一输入形状
- `ledger.constants.ts` 现在是预算状态、草稿来源和更多菜单动作的唯一字符串源，`src/types/ledger.ts` 则是这些对象形状的唯一类型源
- 这一步的作用是先把 V2 的“名词表”收口，后续只需要在校验、存储和统计层围绕这些命名继续推进即可

### Step 3 Completed

- 已在 `ledger.schema.ts` 建立预算与草稿的统一校验层，覆盖月度预算、分类预算、OCR/快捷指令草稿识别、草稿确认和最终记账记录的基础校验
- 已将月度预算和分类预算的金额校验统一到同一套正数与小数位规则上，避免后续在不同页面重复写校验分支
- 已为草稿识别结果增加“必须存在可用字段”的判断，避免空草稿直接进入确认流
- 已为草稿确认补充最终保存约束，确保它仍然要通过记账记录的基础校验，再进入正式创建流程
- 已新增 `ledger.schema.test.ts`，把合法预算、非法预算、可用草稿、空草稿、草稿确认和记录基础校验都纳入测试

### Verification

- `npm run typecheck` 通过
- `npm test` 通过

### Notes

- 这一步把“校验边界”收口到了 schema 层，后续预算页、本地存储读取和草稿确认界面可以直接复用
- 草稿识别现在明确区分“有用信息”和“空壳数据”，避免后续确认页处理无意义输入
- 草稿确认被约束为创建新记录的流程，不会和现有手动编辑语义混淆

### Step 4 Completed

- 已在 `src/lib` 中补齐预算与展示相关纯函数，新增了预算进度计算、分类月度支出汇总、首页预算文案生成，以及草稿金额/时间展示格式化能力
- 已把月度预算的“未设置 / 正常 / 超支”三种状态统一成可直接给首页和预算面板消费的摘要结构，减少页面层重复拼接文案
- 已把分类预算的汇总逻辑从 UI 中剥离出来，确保后续预算面板可以直接消费已经算好的分类摘要
- 已新增 `src/lib/ledger-tools.ts` 与 `src/lib/ledger-tools.test.ts`，为预算进度、分类统计和草稿展示建立可复用测试覆盖

### Verification

- `npm run typecheck` 通过
- `npm test` 通过

### Notes

- 这一步完成后，首页和预算面板所需的基础计算能力已经集中到 `src/lib`
- 页面层后续只需要消费摘要结果，不应再自己计算预算进度或分类汇总
- 草稿确认页后续也可以直接复用这一步的金额与时间展示格式，避免文案和格式分叉

### Step 5 Completed

- 已扩展 `storage-service.ts` 支持预算设置的本地持久化，新增了预算设置的读取、写入和清理能力
- 已把预算设置独立到新的本地存储键位，和现有记账记录、表单偏好保持完全分离，避免数据互相污染
- 已为预算数据增加损坏兜底逻辑，读取到异常数据时会回退到默认预算设置，而不会让页面崩溃
- 已为预算读写补充测试覆盖，验证了正常写读、损坏数据回退和清理行为

### Verification

- `npm run typecheck` 通过
- `npm test` 通过

### Notes

- 预算数据现在已经可以稳定持久化，后续第 6 步可以直接在状态层消费这组存储接口
- 预算设置和账单记录已经在存储层完成分表式隔离，后续扩展分类预算或草稿状态时也应继续沿用这种单独 key 的方式
- 默认预算值仍由存储层兜底提供，页面层可以根据状态层策略再决定是展示默认值还是引导提示

### Step 6 Completed

- 已在 `ledger.store.ts` 中扩展账本状态快照，加入预算设置、本月预算进度、分类预算摘要和预算文案摘要
- 已让状态层在 hydration 和记录变更后统一刷新预算派生状态，使首页和预算页后续可以直接消费一份完整 snapshot
- 已将记录数据和预算设置数据同时纳入状态层读取流程，预算值来自存储层，展示摘要来自 `src/lib/ledger-tools.ts`
- 已为状态层补充测试，验证了预算设置读取、预算进度计算、分类预算摘要和记录变更后的同步刷新

### Verification

- `npm run typecheck` 通过
- `npm test` 通过

### Notes

- 现在状态层已经成为首页与预算页共同依赖的单一入口，后续页面不应再各自读取预算存储或重新计算预算摘要
- 预算进度和分类预算摘要都已经被收敛到 snapshot 中，后续第 7 步可以直接把这些内容接到首页仪表盘布局里
- 这一步只做状态快照的扩展，还没有开始首页重做或预算面板视觉实现

### Step 7 Completed

- 已将首页统计区从预算占位升级为真实预算进度环，预算环现在会根据月预算与本月支出绘制 conic-gradient 进度
- 已在 `LedgerStatsPanel.tsx` 中把月预算金额、进度百分比和剩余/超支文案直接显示在环内，让首页一眼能看到预算使用情况
- 已把预算进度从卡片背景里拆出来，改成中心圆外侧的独立外圈圆环，避免“用框当进度”的视觉误读
- 已把圆环外的卡片区域改成自上而下的预算填充层，让框内其它部分也能直观看出预算消耗的方向与占比
- 已把圆环起点固定在 12 点钟方向，保持外圈从顶部开始顺时针推进，和常见进度环的阅读习惯一致
- 已针对超支状态收紧圆心底部文案，确保“超支 ¥x,xxx.xx”在小圆里也能完整显示
- 已继续收紧首页单屏布局，把预算环维持在统计区内，同时保留中间 `+ 记一笔` 锚点和最近 3 条记录预览
- 已新增 `LedgerStatsPanel.test.tsx`，防止后续改动把预算进度、金额文本或环进度样式变量改坏

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- 已在 393 × 740 的浏览器预览里确认外圈圆环、中心文案和最近三条记录都能稳定落位

### Notes

- 这一步把首页的预算环从“将来再接”的占位状态变成了真实进度表达
- 预算环现在是“中心圆 + 外圈进度环”的双层结构，后续如果再做品牌化视觉，也应该优先在这个层次上扩展，而不是回到卡片框背景
- 外圈之外的卡片区域现在也承担预算表达，采用自上而下的填充层来辅助阅读进度，不再依赖方向箭头提示
- 圆环的起点固定在顶部，超支状态则通过更紧凑的圆心文案维持完整可读性，这两条规则后续都应继续保留
- 首页现在已经同时承接预算进度、今日/本月支出、快速记一笔和最近三条预览，仍然保持单屏节奏
- 第 8 步可以继续围绕统计与派生逻辑做更深一层的能力，但不需要再回头改首页的预算环结构

## 2026-05-12

### Step 8 Completed

- 已在首页统计区右上角接入“更多”入口，并将其连接到独立的预算管理页，而不是继续把预算编辑堆进首页首屏
- 已新增 `LedgerBudgetView` 作为紧凑型预算管理面板，支持月度总预算查看与修改、分类预算列表、分类计划与实际支出对比，以及进入全部记录页的入口
- 已在 `ledger.store.ts` 中补齐预算设置更新方法，使预算页保存动作能够通过状态层写回本地存储并立刻刷新快照
- 已让首页统计、预算管理页和状态快照共用同一套预算派生结果，避免页面层各自重复计算月预算、分类摘要或剩余/超支文案
- 已新增预算管理页测试和预算设置持久化测试，确认预算页可渲染、预算可保存、快照可同步更新

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- `npm run build` 通过
- 已在本地 Chrome 中打开 `http://localhost:5173/`，确认首页“更多”可进入预算管理页，页面内容正常显示且不是空白页

### Notes

- 第 8 步的重点是“把预算管理从首页剥离成独立但紧凑的承载区”，而不是扩张首页首屏
- `LedgerBudgetView` 现在是预算编辑与分类对比的主要入口，后续如果继续扩展预算能力，应优先在这里增加，而不是让首页变复杂
- `ledger.store.ts` 已经成为预算页保存动作的唯一写回通道，后续预算页或统计页不要再直接碰存储层
- 第 9 步仍然保留给记录区的进一步紧凑化与浏览体验优化，不要在这一步继续扩首页结构

### Step 9 Completed

- 已将最近记录列表拆成 `preview` 和 `full` 两种 surface，首页继续使用 `preview` 只展示最近 3 条并隐藏备注与“点击编辑”，全部记录页使用 `full` 保留备注但限制为列表内滚动
- 已让首页的最近记录区继续保持稳定的三条槽位，避免记录数量变化时卡片高度跟着抖动
- 已让全部记录页的列表内容保持在自己的卡片容器内滚动，页面本身继续维持单屏结构，不再被记录数撑长
- 已把最近记录列表的空状态也做了 surface 分流：首页空态更短，全部记录页空态保留完整说明
- 已补充列表 surface 分流的测试，确保后续不会把首页预览误改成全量布局，或把全部记录页误改回首页密度

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- `npm run build` 通过
- 已在浏览器里复核最近记录的 preview/full 分流，确认首页只露最近 3 条、全部记录页在卡片内展示完整条目

### Notes

- 第 9 步完成后，首页和全部记录页已经从“同一套紧凑样式硬切”变成“同一组件、两种 surface”，后续再调手机密度时更容易区分预览和全量需求
- 本次验证时曾临时写入 4 条测试记录用于浏览器复核，相关数据已经用于确认布局，不需要再额外推进第 10 步前的视觉调整
- 后续若要继续优化浏览体验，优先调整 `LedgerRecentRecordsList` 的 surface 规则，而不是把首页和全部记录页重新拆成两套独立列表实现

### Step 10 Completed

- 已将预算管理页的编辑交互收敛为“点齿轮进入编辑、再次点同一个齿轮保存并退出”的单一操作流，不再保留底部单独的 `保存预算` 按钮
- 已让月预算与分类预算共用同一套 `editingTarget` 交互状态，默认保持只读，只有点击对应齿轮后才切换为可编辑输入框
- 已去掉齿轮按钮下方的独立外框，只保留纯图标，让预算页的行内编辑入口更轻、更紧凑
- 已保留 Enter 键提交行为，确保键盘输入结束后也能沿用同一套保存逻辑
- 已补充预算页测试，覆盖齿轮入口、预算文案和底部保存按钮移除后的页面结构

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- `npm run build` 通过

### Notes

- 预算管理页现在的交互原则是“编辑与保存合并在同一个齿轮入口里”，页面上不再额外放一个容易抢空间的保存 CTA
- 月预算和分类预算的编辑状态统一由 `editingTarget` 管理，后续如果再扩预算项，也应继续沿用这种“单一目标、单一入口”的模式
- 齿轮按钮已经从卡片式按钮退化为纯图标，以便保留更多纵向空间给分类预算列表

## 2026-05-16

### Step 11 Completed

- 已实现截图导入草稿承接流程，首页“更多”菜单新增了 `截图导入说明` 入口，可以进入独立的草稿确认面板
- 已把草稿接收逻辑收敛到 `ledger.import-draft.ts`，支持从 URL 查询参数读取结构化草稿、归一化字段、生成表单初值、校验草稿完整性，并将确认内容转换成最终记账记录
- 已新增 `LedgerImportDraftView` 作为草稿确认页，页面会展示金额、商户名、时间、分类候选和支付方式，并允许用户人工修正后再确认保存
- 已保留 `改用手动记账` 的兜底路径，确保草稿信息不完整或识别不理想时仍可回到现有手动流程
- 已把草稿保存链路接入账本状态层，确认后会通过既有记录写入流程生成正式记录

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- `npm run build` 通过
- 已在浏览器中确认 `?draft=...` 可以直接打开草稿页，草稿内容可编辑，确认面板与手动入口都正常显示

### Notes

- 这一步只完成了“截图识别结果 -> 结构化草稿 -> 人工确认保存”的应用承接，不包含系统级 OCR 图片识别本身
- `ledger.import-draft.ts` 现在是草稿归一化、表单状态、提示文案和记录转换的唯一 helper 层，后续如果继续扩展识别能力，也应该先从这里接入
- 草稿页和预算页都属于“先接收结构化数据，再允许人工修正”的高密度承接面，后续文案收紧时要优先保持这两个页面的单屏可读性

### Step 12 Completed

- 已统一收紧首页、预算页、全部记录页和草稿页的空状态、错误状态与微文案，减少“说明书式”长句，改为更短、更直接的手机工具表达
- 已将预算摘要文案从“预算待设置 / 本月支出 ... 预算使用 ...”收紧为“去设置预算 / 设置后再看本月进度 / 本月已用 xx% / 超支 xx”，保证首页和预算页都能在更小空间内保持清晰
- 已把最近记录和全部记录的空状态压缩为 `暂无记录 / 先记一笔`，避免空列表区域在窄屏上显得松散又冗长
- 已把草稿页的提示层分成“没识别到内容”“还差几项”“可直接保存，也可先改字段”三种更短的状态，并统一把 `待识别...` 改成 `待补...`
- 已统一收紧存储异常提示，如 `当前环境不支持本地保存`、`本地记录已损坏，已忽略`、`预算数据已损坏，已回退默认值`，让异常信息更明确但不打断主流程
- 已同步更新对应测试断言，确保后续改动不会把这些状态文案又拉回过长表达

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- `npm run build` 通过

### Notes

- 第 12 步的重点不是新增能力，而是把已有预算、记录、草稿和存储异常的反馈压缩到 V2 需要的密度
- `ledger-tools.ts` 现在不仅提供预算和草稿展示计算，也开始承担“首页摘要文案长度控制”的职责
- 草稿页当前已经能明确区分“完全没识别到内容”和“识别到部分字段但还差几项”，后续如果真接 OCR，这个状态分层可以直接复用

## 2026-05-21

### Step 13 Completed

- 已补齐 V2 收尾阶段缺的壳层回归测试，新增 `LedgerShell.test.tsx`，专门覆盖首页默认落点、导入草稿入口和状态提示透传这三条最容易被页面切换改坏的链路
- 已确认预算与统计纯函数、存储服务、状态层快照、新增/编辑/删除主流程、预算修改流程、草稿确认流程这几类核心逻辑都已有自动化保护，并在本步统一复跑
- 已完成手机端关键页面的手工验收，按当前 localhost 版本实际检查了首页、预算页、全部记录页和截图导入草稿页的进入与切换
- 已确认首页“更多”可以继续进入预算管理页与截图导入草稿页，预算页可进入全部记录页，草稿页可看到预填字段、手动记账兜底和确认保存入口
- 已保持第 13 步只做稳定性收口，没有提前进入后续新功能或新页面扩展

### Verification

- `npm run typecheck` 通过
- `npm test` 通过
- `npm run build` 通过
- 当前共 `15` 个测试文件、`59` 个测试全部通过
- 已在浏览器中手工验证首页、预算页、记录页和草稿流程的关键入口与页面切换

### Notes

- 第 13 步的重点不是补“所有 UI 自动化”，而是补上 V2 真正关键的壳层闭环，确保页面切换、草稿入口和状态提示不会在后续迭代里悄悄失效
- `LedgerShell.test.tsx` 现在承担的是“组合层回归保护”，它不重复测试各个子组件内部细节，而是专门验证壳层是否把首页、草稿和状态提示正确接起来
- 到这一步，V2 已经具备“自动化逻辑保护 + 手机端首屏与关键交互验收 + 稳定构建结果”三条交付条件
